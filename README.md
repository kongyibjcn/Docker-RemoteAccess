# Docker-RemoteAccess
This project used to implement the Remote Docker container access.

In current source code, it already can implement access Docker container using local STDIO and STDOUT...

But the goal is that we should show the stream on web browser

The core part to get docker container's stream dependence on project github.com/fsouza/go-dockerclient


